# Production environment for AI Factory Validation Portal
# Copy to .env.production on the server and adjust values if needed.

PORT=3000
NODE_ENV=production
PYTHONUNBUFFERED=1

# Production defaults to AI_FACTORY_AUTH_REQUIRED=true when NODE_ENV=production.
# Create values out-of-band; do not commit real reviewer credentials.
AI_FACTORY_AUTH_REQUIRED=true
AI_FACTORY_SESSION_SECRET='replace-with-at-least-32-random-characters'
AI_FACTORY_REVIEWER_EMAIL=reviewer@example.invalid
AI_FACTORY_REVIEWER_PASSWORD_HASH='scrypt$replace-with-salt$replace-with-derived-key'
AI_FACTORY_SESSION_TTL_SECONDS=3600
AI_FACTORY_COOKIE_SECURE=true

# Optional deployment-only verification token. Leave empty unless running
# authenticated deploy/verify.sh checks from localhost or CI.
AI_FACTORY_AUTH_TEST_BYPASS_TOKEN=
