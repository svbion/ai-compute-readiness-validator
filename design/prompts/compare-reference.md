# Compare reference versus implementation

Compare the reference image and current implementation screenshot for layout, dimensions, spacing, typography, hierarchy, cards, charts, borders, glow, navigation, table density, missing sections, excess sections, and responsive behavior. Then make the required code changes; do not stop at written feedback. Preserve behavior and re-run validation after edits.
