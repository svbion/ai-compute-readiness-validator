# Create an implementation screenshot

Use `npm run design:screenshot -- --route=<route> --name=<page-id> --viewport=1536x1024`. Ensure authenticated routes log in with deterministic reviewer credentials, fonts and charts have settled, animations are reduced, unstable timestamps are hidden or documented, and output lands in `design/implementation-screenshots/current/<page-id>-1536x1024.png`.
