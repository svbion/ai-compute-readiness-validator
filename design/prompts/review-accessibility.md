# Review GPUValidator accessibility

Check keyboard navigation, contrast, focus visibility, form labels, table semantics, chart alternatives, reduced-motion support, modal focus trapping, drawer behavior, and screen-reader naming. Critical operational states must not rely on color alone. Fix code where possible and document remaining blockers.
