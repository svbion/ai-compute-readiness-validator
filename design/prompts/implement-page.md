# Implement a GPUValidator page from reference

Use one page reference as canonical composition and `docs/DESIGN_SYSTEM.md` as canonical styling. Preserve existing functionality and real API integrations. Reuse the shared application shell; do not create duplicate sidebars, topbars, cards, buttons, or status components. Use typed fixtures only when no backend exists and label them as non-live. Add or update tests, capture a screenshot, compare with the reference, and report differences honestly.

Inputs required: page ID, route, reference image, current implementation route, acceptance criteria.
