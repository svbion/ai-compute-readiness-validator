# Audit GPUValidator design drift

Scan the repository for hard-coded colors, duplicate card styles, inconsistent radius, page-specific sidebar implementations, duplicate buttons, inconsistent semantic status colors, arbitrary shadows, non-token spacing, and chart palette drift. Return findings with file paths and proposed code changes, then patch issues that are safe and scoped.
